blockchain-to-bumfuzzle
=============

![](logo.png)

Chrome extension that replaces occurrences of 'Blockchain' with 'Bumfuzzle'

Installation
------------

In Chrome, choose Window > Extensions.  Drag CloudToButt.crx into the page that appears.
